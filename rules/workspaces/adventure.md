# Adventure Workspace Rules

## Purpose

Define defaults that should apply to every `adventure` workspace in `rpg-engine`.

## Defaults

- no adventure-wide defaults are required yet beyond the broader product and engine rules

## Principle

Add only rules that truly belong to every `adventure` workspace.
