# World Workspace Rules

## Purpose

Define defaults that should apply to every `world` workspace in `rpg-engine`.

## Defaults

- no global world-specific defaults are required yet beyond the broader product and engine rules

## Principle

Add only rules that truly belong to every `world` workspace.
